#!/bin/bash
#
# Git Pre-Commit Hook: Hexagonal Architecture Validation
#
# This hook prevents commits that would break the hexagonal architecture
# forwarding chain. It runs comprehensive validation to ensure all impl/*.py
# files properly forward to handlers.py implementations.
#
# Automatically installed by: scripts/setup_architecture_validation_hook.sh
#
# To bypass this hook (NOT RECOMMENDED):
#   git commit --no-verify
#

set -e

echo ""
echo "🔍 Running Hexagonal Architecture Validation..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Run comprehensive validation script
if python3 scripts/validate_handler_forwarding_comprehensive.py; then
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "✅ Architecture validation PASSED"
    echo "✅ Commit allowed"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    exit 0
else
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "❌ Architecture validation FAILED"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    echo "🚨 COMMIT BLOCKED: Broken forwarding chain detected!"
    echo ""
    echo "Your changes would break the hexagonal architecture, causing 501 errors"
    echo "despite having working implementations in handlers.py."
    echo ""
    echo "📋 REQUIRED ACTIONS:"
    echo ""
    echo "1. Fix the broken forwarding chains:"
    echo "   python3 scripts/post_openapi_generation.py backend/api/src/main/python"
    echo ""
    echo "2. Verify the fix:"
    echo "   python3 scripts/validate_handler_forwarding_comprehensive.py"
    echo ""
    echo "3. Try your commit again:"
    echo "   git commit"
    echo ""
    echo "⚠️  To bypass this validation (NOT RECOMMENDED):"
    echo "   git commit --no-verify"
    echo ""
    echo "   WARNING: Bypassing will likely cause Bugs #1 and #7 to recur!"
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    exit 1
fi
