connexion[swagger-ui] >= 2.6.0; python_version>="3.6"
# 2.3 is the last version that supports python 3.4-3.5
connexion[swagger-ui] <= 2.3.0; python_version=="3.5" or python_version=="3.4"
# prevent breaking dependencies from advent of connexion>=3.0
connexion[swagger-ui] <= 2.14.2; python_version>"3.4"
# connexion requires werkzeug but connexion < 2.4.0 does not install werkzeug
# we must peg werkzeug versions below to fix connexion
# https://github.com/zalando/connexion/pull/1044
werkzeug == 0.16.1; python_version=="3.5" or python_version=="3.4"
swagger-ui-bundle >= 0.0.2
python_dateutil >= 2.6.0
setuptools >= 21.0.0
Flask == 2.1.1
flask-cors >= 3.0.10

# AWS SDK and ORM
boto3 >= 1.26.0
pynamodb >= 5.5.0

# OpenAI for ChatGPT integration, content moderation, and Assistants API with Vector Stores
openai >= 1.3.0

# Additional dependencies for safety and personalization features
tiktoken >= 0.5.0  # Token counting for context optimization
redis >= 4.5.0     # Caching for context and guardrails
tenacity >= 8.2.0  # Retry logic for OpenAI API calls

# Testing dependencies
flask-testing >= 0.8.0  # Flask test case framework for unit tests
